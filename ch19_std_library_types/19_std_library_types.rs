// Std library types

// Std library has many custom types that expand drastically upon the primitives, such as
// - growable `String`s, vectors, optional types (`Option<i32>`), error handling types (`Result<i32,i32>`), heap allocated pointers (Box<i32>)